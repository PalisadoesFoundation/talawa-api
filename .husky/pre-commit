
# Disable the hooks in CI 
[ -n "$CI" ] && exit 0

# Change to the current directory

# Run your commands
npm run generate-docs
npm run format:fix
# npm run lint:fix
npm run lint-staged
npm run typecheck
npm run update:toc

git add .
