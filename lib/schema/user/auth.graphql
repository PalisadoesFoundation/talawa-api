

module.exports = `


  input LoginInput {
    email:String!, 
    password:String!
    token: String
  }
  
  type AuthData {
    user: User!,
    accessToken: String!
    refreshToken: String!
  }

  type ExtendSession {
    accessToken: String!
    refreshToken: String!
  }


`