version: "3"

services:
  db:
    image: mongo
    restart: always
    volumes:
      - ./data:/data/db
    ports:
      - "27017:27017"

  app:
    build: .
    restart: always
    ports:
      - "4000:4000"
    environment:
      MONGO_URI: mongodb://db:27017/talawa
    depends_on:
      - db
