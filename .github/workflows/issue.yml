name: Issue Workflow
on:
  issues:
    types: ['opened']
jobs:
 issue-auto-label:
    uses: PalisadoesFoundation/.github/.github/workflows/issue.yml@feat/main
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}