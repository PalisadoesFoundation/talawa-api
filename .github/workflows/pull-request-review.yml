name: Pull Request Review

on:
  pull_request_review:
    types: [submitted, edited, dismissed]

jobs:
  Check-CodeRabbit-Approval:
    uses: PalisadoesFoundation/.github/.github/workflows/pr-review-coderabbit.yml@main
    secrets:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
