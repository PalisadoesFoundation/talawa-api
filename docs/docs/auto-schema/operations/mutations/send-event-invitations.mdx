---
id: send-event-invitations
title: sendEventInvitations
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Send email invitations to one or more addresses for an event.

```graphql
sendEventInvitations(
  input: SendEventInvitationsInput!
): [EventInvitation!]
```

### Arguments

#### [<code style={{ fontWeight: 'normal' }}>sendEventInvitations.<b>input</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>SendEventInvitationsInput!</b></code>](/auto-schema/types/inputs/send-event-invitations-input.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="input"/>

### Type

#### [<code style={{ fontWeight: 'normal' }}><b>EventInvitation</b></code>](/auto-schema/types/objects/event-invitation.mdx) <Badge class="badge badge--secondary " text="object"/>

An invitation to an event sent to an email address.
