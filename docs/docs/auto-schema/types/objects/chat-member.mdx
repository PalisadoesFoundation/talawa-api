---
id: chat-member
title: ChatMember
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

A member of a chat with their role.

```graphql
type ChatMember {
  role: ChatMembershipRole
  user: User
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>ChatMember.<b>role</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>ChatMembershipRole</b></code>](/auto-schema/types/enums/chat-membership-role.mdx) <Badge class="badge badge--secondary " text="enum"/>

The role of the user in the chat.

#### [<code style={{ fontWeight: 'normal' }}>ChatMember.<b>user</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>User</b></code>](/auto-schema/types/objects/user.mdx) <Badge class="badge badge--secondary " text="object"/>

The user who is a member of the chat.

### Member Of

[`ChatMembersConnectionEdge`](/auto-schema/types/objects/chat-members-connection-edge.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/>
