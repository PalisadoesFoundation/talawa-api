---
id: query-sign-in-input
title: QuerySignInInput
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

```graphql
input QuerySignInInput {
  emailAddress: EmailAddress!
  password: String!
  recaptchaToken: String
}
```

### Fields

#### [<code style={{ fontWeight: 'normal' }}>QuerySignInInput.<b>emailAddress</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>EmailAddress!</b></code>](/auto-schema/types/scalars/email-address.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/>

Email address of the user.

#### [<code style={{ fontWeight: 'normal' }}>QuerySignInInput.<b>password</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>String!</b></code>](/auto-schema/types/scalars/string.mdx) <Badge class="badge badge--secondary badge--non_null" text="non-null"/> <Badge class="badge badge--secondary " text="scalar"/>

Password of the user to sign in to talawa.

#### [<code style={{ fontWeight: 'normal' }}>QuerySignInInput.<b>recaptchaToken</b></code>](#)<Bullet />[<code style={{ fontWeight: 'normal' }}><b>String</b></code>](/auto-schema/types/scalars/string.mdx) <Badge class="badge badge--secondary " text="scalar"/>

Optional reCAPTCHA token for verification (required when server-side reCAPTCHA is enabled)

### Member Of

[`signIn`](/auto-schema/operations/queries/sign-in.mdx) <Badge class="badge badge--secondary badge--relation" text="query"/>
