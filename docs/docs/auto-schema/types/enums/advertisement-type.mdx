---
id: advertisement-type
title: AdvertisementType
---

export const Bullet = () => (
  <>
    <span
      style={{
        fontWeight: "normal",
        fontSize: ".5em",
        color: "var(--ifm-color-secondary-darkest)",
      }}
    >
      &nbsp;●&nbsp;
    </span>
  </>
);

export const SpecifiedBy = (props) => (
  <>
    Specification
    <a
      className="link"
      style={{ fontSize: "1.5em", paddingLeft: "4px" }}
      target="_blank"
      href={props.url}
      title={"Specified by " + props.url}
    >
      ⎘
    </a>
  </>
);

export const Badge = (props) => (
  <>
    <span className={props.class}>{props.text}</span>
  </>
);

import { useState } from "react";

export const Details = ({
  dataOpen,
  dataClose,
  children,
  startOpen = false,
}) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details
      {...(open ? { open: true } : {})}
      className="details"
      style={{
        border: "none",
        boxShadow: "none",
        background: "var(--ifm-background-color)",
      }}
    >
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle: "none" }}
      >
        {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};

Possible variants of the type of an advertisement.

```graphql
enum AdvertisementType {
  banner
  menu
  pop_up
}
```

### Values

#### [<code style={{ fontWeight: 'normal' }}>AdvertisementType.<b>banner</b></code>](#)

#### [<code style={{ fontWeight: 'normal' }}>AdvertisementType.<b>menu</b></code>](#)

#### [<code style={{ fontWeight: 'normal' }}>AdvertisementType.<b>pop_up</b></code>](#)

### Member Of

[`Advertisement`](/auto-schema/types/objects/advertisement.mdx) <Badge class="badge badge--secondary badge--relation" text="object"/><Bullet />[`MutationCreateAdvertisementInput`](/auto-schema/types/inputs/mutation-create-advertisement-input.mdx) <Badge class="badge badge--secondary badge--relation" text="input"/><Bullet />[`MutationUpdateAdvertisementInput`](/auto-schema/types/inputs/mutation-update-advertisement-input.mdx) <Badge class="badge badge--secondary badge--relation" text="input"/>
