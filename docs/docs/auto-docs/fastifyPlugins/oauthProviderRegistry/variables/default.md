[API Docs](/)

***

# Variable: default()

> **default**: (`fastify`) => `Promise`\<`void`\>

Defined in: [src/fastifyPlugins/oauthProviderRegistry.ts:19](https://github.com/PalisadoesFoundation/talawa-api/tree/mainsrc/fastifyPlugins/oauthProviderRegistry.ts#L19)

Fastify plugin that initializes and registers the OAuth provider registry.
This plugin builds the registry from configuration and attaches it to the fastify instance.

## Parameters

### fastify

`FastifyInstance`

## Returns

`Promise`\<`void`\>
